void textHoverOver() {
  /*
  
  //
  if ( mouseX>=quitButtonX && mouseX<=quitButtonX+quitButtonWidth && mouseY>=quitButtonY && mouseY<=quitButtonY+quitButtonHeight ) {
    ExitHO();
  } else {
    image(); //this is for the (X, Y, Width, Height) and will make the image appear when the text is not there
  }//End quit
  //
  if (  ) ;
  */
}//End textHoverOver
